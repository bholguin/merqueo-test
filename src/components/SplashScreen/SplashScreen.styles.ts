import styled from "styled-components";
import tw from "twin.macro";

export const StyledContainer = styled.div`
  ${tw`w-full h-screen bg-[rgba(255, 255, 255, 0.5)] flex justify-center items-center fixed z-index[50000]`}
`;
